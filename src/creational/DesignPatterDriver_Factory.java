package creational;

public class DesignPatterDriver_Factory {
/*
 * Any change in the object class will lead to changes in the 
 * model/object class and the factory class but the user class
 *  need not bring in any changes which makes it reusable
 *  and reduces the overhead of object creation from client code
 */
	public static void main(String[] args) {
	//	Laptop dell=LaptopFactory.getLaptop(DeviceType.MAC);
		//System.out.println(dell.getConfiguration());
		//System.out.println(dell.toString());
	}

}
